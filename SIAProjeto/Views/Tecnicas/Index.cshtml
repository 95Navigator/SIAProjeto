@model IEnumerable<SIAProjeto.Models.Tecnica>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.idUtilizador)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.idUtilizador)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idTecnica }) |
            @Html.ActionLink("Details", "Details", new { id=item.idTecnica }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idTecnica })
        </td>
    </tr>
}

</table>
